--- tflite-micro-main/tensorflow/lite/micro/kernels/activation_utils.h  2025-04-04 19:16:17.000000000 +0200
+++ tflite-micro-main/tensorflow/lite/micro/kernels/activation_utils2.h 2025-06-08 05:39:13.292033800 +0200
@@ -40,11 +40,11 @@
     case kTfLiteActRelu6:
       return TfLiteMax(0.0f, TfLiteMin(a, 6.0f));
     case kTfLiteActTanh:
-      return std::tanh(a);
+      return tanh(a);
     case kTfLiteActSignBit:
       return std::signbit(a);
     case kTfLiteActSigmoid:
-      return 1.0f / (1.0f + std::exp(-a));
+      return 1.0f / (1.0f + exp(-a));
   }
   return 0.0f;  // To indicate an unsupported activation (i.e. when a new fused
                 // activation is added to the enum and not handled here).
